apiVersion: v1
kind: Service
metadata:
  name: {{ include "xapp-traffic-steering.fullname" . }}-http
  labels:
    {{- include "xapp-traffic-steering.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "xapp-traffic-steering.fullname" . }}
  ports:
    - name: http-health
      port: {{ .Values.service.httpPort }}
      targetPort: {{ .Values.service.httpPort }}
    - name: http-metrics
      port: {{ .Values.service.metricsPort }}
      targetPort: {{ .Values.service.metricsPort }}
